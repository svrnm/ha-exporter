{
  "name": "ha-exporter-server",
  "version": "0.1.0",
  "description": "Dahoamboard server — receives sensor / statistics payloads from the HA Exporter Home Assistant integration and stores them in SQLite.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "eslint . --max-warnings 0",
    "generate-tokens": "node scripts/generate-tokens.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "globals": "^17.5.0"
  }
}
